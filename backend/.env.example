# Database Configuration
# Get your connection string from Neon Dashboard: https://neon.tech
DATABASE_URL='postgresql://neondb_owner:npg_KuFw58pkIMDG@ep-fancy-bird-ahkak717-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# Authentication Secret (CRITICAL - MUST match frontend secret)
# Generate with: openssl rand -base64 64 | tr -d '\n'
# IMPORTANT: This MUST be identical in both backend/.env and frontend/.env.local
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-required

# CORS Configuration
# Frontend origin(s) allowed to access the API
ALLOWED_ORIGINS=http://localhost:3000

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Environment
ENVIRONMENT=development
